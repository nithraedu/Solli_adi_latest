<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/progress">
       <!-- <shape
            android:innerRadiusRatio="500"
            android:shape="ring"
            android:thicknessRatio="3.0"

            android:useLevel="false"
            android:visible="true">
            <gradient
                android:centerColor="#FF00"
                android:endColor="#FF00"
                android:startColor="#FF00ff"
                android:type="sweep" />
        </shape>-->
        <rotate
            android:fromDegrees="270"
            android:toDegrees="-90">
            <shape
                android:innerRadiusRatio="460"
                android:shape="ring"
                android:thicknessRatio="3.0"

                android:useLevel="true"
                android:visible="true">
                <gradient
                    android:centerColor="#026025"
                    android:endColor="#026025"
                    android:startColor="#026025"
                    android:type="sweep" />
            </shape>
        </rotate>
    </item>
</layer-list>
